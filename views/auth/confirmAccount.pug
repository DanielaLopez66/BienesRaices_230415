extends ../layout/index

block contenido
    div.py-10
        h1.text-center.font-extrabold.my-10.text-4xl.text-amarillo Bienes 
            span.font-normal.text-black Raíces
        h2.text-center.font-extrabold.text-2xl.text-black= page

    div(class="bg-white shadow py-8 px-4 rounded-lg max-w-md mx-auto")
        p(class=error ? 'bg-rojo' : 'bg-verde').py-2.px-5.rounded-lg.w-full.font-bold.text-center.text-black.uppercase= msg

        if !error
            a(href="/auth/login" class="text-center font-bold text-lg text-slate-600 uppercase mt-10 block bg-verde py-4 px-8 rounded-lg") Ya Puedes Iniciar Sesión 
        else
            a(href="/auth/login" class="text-center font-bold text-lg text-white uppercase mt-10 block bg-rojo py-4 px-8 rounded-lg") Volver al inicio
